# 🌀 Quote Carousel Component

## نظرة عامة

مكون **Quote Carousel** هو كارت أنيق ومتحرك لعرض الاقتباسات (Quotes) بتصميم Glass morphism احترافي مع تأثيرات fade-in/out سلسة.

## ✨ المميزات

- ✅ **تأثيرات احترافية**: fade-in/out مع blur للنعومة القصوى
- ✅ **تبديل تلقائي**: يتغير الاقتباس كل بضع ثواني (قابل للتخصيص)
- ✅ **إيقاف مؤقت ذكي**: يتوقف التبديل عند hover على الكارت
- ✅ **مؤشرات تفاعلية**: dots للتنقل اليدوي بين الاقتباسات
- ✅ **أيقونات متحركة**: علامات اقتباس تزيينية مع حركة خفيفة
- ✅ **مؤشر الحالة**: يعرض حالة التشغيل (Auto/Paused)
- ✅ **Glass morphism**: تصميم عصري يتماشى مع البورتفوليو
- ✅ **Responsive**: يعمل بشكل مثالي على جميع الشاشات
- ✅ **متوافق مع معايير التصميم**: يستخدم em/rem للوحدات

## 📁 الملفات

```
src/
├── components/ui/
│   └── quote-slider.tsx          # المكون الرئيسي
├── data/
│   └── quotes.ts                 # بيانات الاقتباسات
├── components/sections/
│   └── quote-section.tsx         # قسم الاقتباسات في الصفحة الرئيسية
└── app/
    └── quotes-test/
        └── page.tsx              # صفحة تجريبية
```

## 🎯 طريقة الاستخدام

### الاستخدام الأساسي

```tsx
import QuoteSlider from "@/components/ui/quote-slider";
import { inspirationalQuotes } from "@/data/quotes";

export default function MyPage() {
  return (
    <div className="max-w-[50em] mx-auto">
      <QuoteSlider quotes={inspirationalQuotes} />
    </div>
  );
}
```

### مع خيارات مخصصة

```tsx
<QuoteSlider
  quotes={myQuotes}
  interval={7000} // 7 ثواني لكل اقتباس
  autoPlay={true} // تفعيل التبديل التلقائي
/>
```

### بدون تبديل تلقائي

```tsx
<QuoteSlider
  quotes={myQuotes}
  autoPlay={false} // تحكم يدوي فقط
/>
```

## 🔧 Props

| Prop       | Type          | Default   | Description                   |
| ---------- | ------------- | --------- | ----------------------------- |
| `quotes`   | `QuoteItem[]` | **مطلوب** | مصفوفة الاقتباسات             |
| `interval` | `number`      | `5000`    | مدة عرض كل اقتباس (ملي ثانية) |
| `autoPlay` | `boolean`     | `true`    | تفعيل/إيقاف التبديل التلقائي  |

## 📊 نوع البيانات

```typescript
interface QuoteItem {
  text: string; // نص الاقتباس
  author: string; // اسم المؤلف
  role?: string; // الوظيفة/اللقب (اختياري)
}
```

## 📝 مثال على البيانات

```typescript
export const myQuotes: QuoteItem[] = [
  {
    text: "The only way to do great work is to love what you do.",
    author: "Steve Jobs",
    role: "Co-founder of Apple",
  },
  {
    text: "Code is like humor. When you have to explain it, it's bad.",
    author: "Cory House",
    role: "Software Architect",
  },
];
```

## 🎨 التخصيص

### تغيير الألوان

المكون يستخدم متغيرات CSS من نظام الثيم:

- `text-primary`: لون العناصر التفاعلية
- `text-accent`: لون العناصر الثانوية
- `text-gradient-animated`: تدرج متحرك للاسم

### تخصيص الأنيميشن

يمكنك تعديل مدة الأنيميشن في الكود:

```tsx
// في quote-slider.tsx
transition={{
  duration: 0.6,  // قم بتغيير هذه القيمة
  ease: [0.22, 1, 0.36, 1],
}}
```

## 🚀 الصفحة التجريبية

للاطلاع على المكون بشكل عملي، قم بزيارة:

```
http://localhost:3000/quotes-test
```

تعرض هذه الصفحة:

- مثال على الاقتباسات الملهمة
- مثال على اقتباسات تقنية
- مثال على التحكم اليدوي
- قائمة بجميع المميزات
- أمثلة على الاستخدام

## 🎭 التأثيرات المتحركة

### 1. Fade In/Out

- يظهر الاقتباس الجديد بتأثير fade-in + blur
- يختفي الاقتباس القديم بتأثير fade-out + blur
- انتقال سلس بدون قفزات

### 2. الأيقونات التزيينية

- حركة دورانية خفيفة (+/- 5 درجات)
- تكبير وتصغير طفيف (scale)
- حركة مستمرة بدون توقف

### 3. المؤشرات (Dots)

- تكبير المؤشر النشط
- تأثير glow على المؤشر النشط
- hover effect على باقي المؤشرات

### 4. خط الفصل

- يظهر بتأثير scale من المنتصف
- تدرج لوني من الشفاف إلى primary

## 📱 Responsive Design

المكون مُحسّن لجميع الشاشات:

- **Mobile**: أحجام خطوط أصغر، padding مناسب
- **Tablet**: أحجام متوسطة
- **Desktop**: أحجام كبيرة، مساحات واسعة

## ⚡ الأداء

- استخدام `AnimatePresence` لإدارة الأنيميشن
- تنظيف الـ intervals عند unmount
- تحسين re-renders باستخدام useState

## 🔍 Accessibility

- استخدام `<blockquote>` للاقتباسات
- استخدام `<cite>` للمؤلف
- aria-labels للأزرار
- keyboard navigation support

## 🛠️ الصيانة

### إضافة اقتباسات جديدة

قم بتعديل `src/data/quotes.ts`:

```typescript
export const myNewQuotes: QuoteItem[] = [
  {
    text: "Your quote here",
    author: "Author Name",
    role: "Optional Role",
  },
];
```

### تعديل الفترة الزمنية الافتراضية

في `quote-slider.tsx`:

```typescript
export default function QuoteSlider({
  quotes,
  interval = 5000, // غير هذا الرقم (بالملي ثانية)
  autoPlay = true,
}: QuoteSliderProps);
```

## 🎯 حالات الاستخدام

1. **صفحة رئيسية**: عرض اقتباسات ملهمة
2. **صفحة About**: عرض الفلسفة الشخصية
3. **Blog**: عرض اقتباسات من المقالات
4. **Portfolio**: عرض شهادات العملاء
5. **Landing Page**: عرض القيم والمبادئ

## 🐛 استكشاف الأخطاء

### المكون لا يتبدل تلقائياً

- تأكد من `autoPlay={true}`
- تأكد من وجود أكثر من اقتباس واحد
- تحقق من console للأخطاء

### الأنيميشن متقطع

- تأكد من عدم وجود re-renders كثيرة
- تحقق من أداء الجهاز
- جرب زيادة `interval`

### الصور لا تظهر

- تأكد من مسار البيانات صحيح
- تحقق من import

## 📚 موارد إضافية

- [Framer Motion Docs](https://www.framer.com/motion/)
- [Next.js Documentation](https://nextjs.org/docs)
- [Tailwind CSS](https://tailwindcss.com/)

## ✅ Checklist التطبيق

- [x] إنشاء مكون QuoteSlider
- [x] إنشاء ملف البيانات quotes.ts
- [x] تحديث quote-section.tsx
- [x] إنشاء صفحة تجريبية
- [x] توثيق المكون
- [x] اختبار على جميع الشاشات
- [x] التأكد من Accessibility
- [x] التأكد من الأداء

---

**تم التطوير بواسطة**: Ahmed Abdeltawab  
**التاريخ**: 13 نوفمبر 2025  
**الإصدار**: 1.0.0
